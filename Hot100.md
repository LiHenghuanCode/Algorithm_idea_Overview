# 哈希
## 1. 两数之和  
给定一个整数数组 `nums` 和一个整数目标值 `target`，请你在该数组中找出 和为目标值 `target`  的那 两个 整数，并返回它们的数组下标。  
### 思路重点
a. 建立一个HashMap, 遍历数组，
**HashMap里存 key: 数组元素值，value: 元素值对应下表**。  
b. 遍历时加判断: 如果遍历时发现HashMap里存在这样一个key——`target`减去当前遍历值等于该key值，则输出该key值对应的value, 和当前遍历位置的索引值  
c. 注意：之所以这样存 key: 数组元素值，value: 元素值对应下表。是因为大多数

## 2. 字母异位词分组
给你一个字符串数组，请你将 字母异位词 组合在一起。可以按任意顺序返回结果列表。  
示例 :
```
输入: strs = ["eat", "tea", "tan", "ate", "nat", "bat"]  
输出: [["bat"],["nat","tan"],["ate","eat","tea"]]
```
### 思路重点
a. 建立一个HashMap, 遍历数组，
**HashMap里存 key: 数组元素字符串的Sort后值，value: 存List，List元素满足——Sort后等于Key值**。  
b. 排序先用`toCharArray()`方法吧字符串变成字符数组，再用`Arrays.sort(array)`排序，最后用String构造器输入排序后数组作为参数产生新字符串;  
c. 一个Tricky做法: `List<String> list = map.getOrDefault(key, new ArrayList<String>());`

## 3. 最长连续序列
给定一个未排序的整数数组 nums ，找出数字连续的最长序列（不要求序列元素在原数组中连续）的长度。  
请你设计并实现时间复杂度为 O(n) 的算法解决此问题。  
示例 1：  
```
输入：nums = [100,4,200,1,3,2]
输出：4
解释：最长数字连续序列是 [1, 2, 3, 4]。它的长度为 4。
```

### 思路重点
a. 排序该数组。建立`HashSet`，寄存一个MaxLength值。  
b. 遍历数组，不断检查Set里是否包含当前索引下的元素值-1。如果包含，则继续下一个遍历，如果不包含，比较并更新MaxLength，然后清空Set。  
c. 清空以后的Set无元素，所以每次遍历前要检查Set是否为空，为空就把当前遍历元素加入到Set，并跳过当次遍历。