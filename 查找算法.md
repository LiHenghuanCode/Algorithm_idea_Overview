## 查找
在计算机里，查找的本质是比对，对于一大堆数据，计算机要从这一堆数据中找到name为张三的数据行，只能通过，一行一行地取出数据比对name的字段值是否为张三。

顺序查找（O(n)）
从内存地址 0 号开始，一个一个比。

二分查找（O(log n)）
强制数据有序。利用“大于/小于”这种是非题，每次排除一半数据。然而如果数据没法做大于小于比较，该法也不可取。这是比较模型下的理论极限（信息论决定你至少需要 log₂n 次比较）

哈希查找（平均 O(1)）
直接造一个数学公式（哈希函数），把 ID=100 代入公式，直接算出物理地址。

内存有限（鸽巢原理），无限个键必然对应有限个地址，所以冲突（Collision）不可避免。处理冲突（链地址/开放寻址）的本质，是退回去做微量的顺序或二分查找。